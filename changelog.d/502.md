- (#502) — **Added:** `inbox_list` and `inbox_get` take an optional `readerSessionId`, which records that a session read the items of its own closed asks.
- (#502) — **Added:** `GET /api/projects/{handle}/inbox/asks/{askId}` tells an agent-scoped token whether an inbox ask closed and whether its session read every item.
- (#502) — **Changed:** each `inbox_list` row also returns the item's options, selected options, written answer and close note.
