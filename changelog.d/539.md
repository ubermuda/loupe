- (#539) — **Added:** `loupe init` now offers to name `loupe mcp` in `.mcp.json`, which is the entry that makes an agent in the repository start it. Every other server in that file is kept, and `--mcp` or `--no-mcp` answers without being asked.
- (#539) — **Added:** `loupe init` reports a Claude Code server of the same name that hides the `.mcp.json` it writes, and offers to remove it. Claude Code prefers its own local-scope entry, so the file otherwise has no effect and nothing says so.
