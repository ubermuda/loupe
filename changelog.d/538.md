- (#538) — **Fixed:** `loupe login` now signs in to `https://loupe.ac` by default. It previously defaulted to a local development host, so a release binary could not sign in without `--url`.
- (#538) — **Added:** `LOUPE_URL` sets the instance `loupe login` uses, so a self-hosted or development instance needs the flag only once.
