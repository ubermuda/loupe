- (#536) — **Fixed:** the production image builds again. Warming the cache had outgrown PHP's default memory limit, and the image now has a build stage whose limit does not follow it into production.
- (#536) — **Changed:** the Tailwind binary is fetched by Docker with a pinned checksum rather than downloaded during the build, so a transient GitHub failure no longer fails the whole image build.
