- (#492) — **Changed:** The bridge starts each worker under a new Claude Code session id, and the worker run report now requires `sessionId`. The server refuses a report from a bridge built before this change, so rebuild the bridge.
- (#492) — **Added:** `GET /api/events` returns a `flags` map with `inbox.enabled`, and a bridge on an instance with the inbox on names the session id and the bridge id in the worker prompt.
