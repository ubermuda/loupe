- (#504) — **Added:** a bridge rule on `inbox.ask_closed` with `resume: true` resumes the Claude Code session that asked, on the card of that session, and skips the resume when the session already read every item of its ask.
- (#504) — **Changed:** a bridge rule on `inbox.ask_closed` without `resume: true` now stops the bridge at start.
- (#504) — **Changed:** while the inbox is on, a bridge worker's prompt tells the agent to pass its session id as `readerSessionId` when it reads its answers.
