- (#561) — **Removed:** `GITHUB_WEBHOOK_SECRET` no longer verifies any delivery, so every existing GitHub webhook stops reaching cards after the upgrade, and each project owner must connect the project's repositories again.
- (#561) — **Added:** a project owner connects GitHub repositories on the Connections tab, with a per-project webhook or the GitHub App, and sees the state of each repository.
- (#561) — **Security:** a GitHub delivery reaches only the cards of the project that received it, and only an App installation can make a repository belong to one project.
