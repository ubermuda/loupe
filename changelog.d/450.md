- (#450) — **Changed:** `loupe bridge run` reads a rule file, `rules.yaml` beside the CLI's `config.json`, that says which event, project and column start an agent, with which prompt, in which directory.
- (#450) — **Removed:** `loupe bridge run` no longer takes `--site` or `--dir`, and it refuses to start without a rule file. To upgrade, write `rules.yaml` with one project and a rule on `to: next`, as `cli/README.md` shows.
