- (#526) — **Added:** an OAuth grant can cover every project you own, so a new project needs no new login.
- (#526) — **Added:** a request names the project it acts on in an `X-Loupe-Project` header. A header naming a project your login does not cover is refused rather than ignored.
- (#526) — **Fixed:** a login survives a refresh whose response was lost. A rotated refresh token stays redeemable once for `OAUTH_REFRESH_GRACE_SECONDS`, which defaults to 60 and accepts `0` to switch it off.
