- (#540) — **Added:** `SITE_REVIEW_WIDGET_PROJECT` embeds the site-review widget by project id, so the reviewer signs in through the OAuth popup and the page source carries no widget token. It takes precedence over `SITE_REVIEW_WIDGET_TOKEN`.
