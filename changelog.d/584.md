- (#584) — **Added:** a `loupe bridge` rule with `action: interactive` opens an interactive Claude Code session in a terminal when a card enters its column, through a `launch.command` set in `rules.yaml`, and the Worker runs page shows the launch.
