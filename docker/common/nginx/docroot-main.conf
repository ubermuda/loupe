# Document root for the main checkout. The worktree stacks
# (docker/compose/worktree.yaml) mount a generated file at this same path pointing at
# their own public/ instead, which is the only thing that differs between them
# and the main stack — default.conf itself is shared verbatim.
root /var/www/html/public;
