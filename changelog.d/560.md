- (#560) — **Added:** A `document.review_submitted` event now names the stage card and its column, and a bridge rule can match it on `verdict`, so a request for changes on a stage document starts a fix-round worker with no card move.
- (#560) — **Changed:** A bridge rule on `document.review_submitted` now skips a verdict that names no stage card, where it matched every verdict of the project before.
