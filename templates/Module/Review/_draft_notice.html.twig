<div class="lp-form-hint" hidden data-form-draft-target="notice">
    <p>{{ 'review.draft.retained'|trans }}</p>
    <button type="button" class="lp-btn lp-btn--ghost lp-btn--sm" data-action="form-draft#discard">{{ 'review.draft.discard'|trans }}</button>
</div>
<p class="lp-field-errors" hidden data-form-draft-target="stale" role="status">{{ 'review.draft.stale'|trans }}</p>
