- (#573) — **Added:** `loupe bridge` runs hook packages on its start, stop, busy and idle events, installed from GitHub at a pinned commit with `loupe bridge hooks install`.
- (#573) — **Added:** the Rules page shows the last run of each bridge hook, with its outcome and its last error.
- (#573) — **Added:** the Amphetamine hook package keeps a Mac awake, lid closed included, while the bridge has work.
