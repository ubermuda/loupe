- (#563) — **Added:** `loupe bridge reload` applies a changed `rules.yaml` to a running bridge with no restart, and a failed reload keeps the old rules.
- (#563) — **Added:** `rules.yaml` takes a `defaults:` block that sets `permissionMode` and `model` for every rule that sets none.
- (#563) — **Changed:** `loupe mcp` reads `.loupe.yaml` again when the file changes, so the next request names the new project.
- (#563) — **Changed:** A second `loupe bridge run` on the same rule file refuses to start.
