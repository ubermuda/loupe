- (#513) — **Added:** Each board column has a colour you pick from twelve in board settings, and a new column starts with one no other column uses.
- (#513) — **Changed:** Cards open, and are added and edited, in a drawer that slides in beside the board, and the notifications drawer slides in the same way.
- (#513) — **Changed:** The topbar search sits behind the `search.topbar.enabled` flag, which is off by default.
- (#513) — **Changed:** An inbox ask shows its first item's title as its heading, and a presence dot on the agent shows whether its bridge is live, with the session in a tooltip.
- (#513) — **Changed:** The card overview lays its fields out as a grid and lists the agent runs on the card.
- (#513) — **Changed:** Site review, Rules, All inboxes and About follow the new visual design, and a site review comment lists its linked work in a table.
- (#513) — **Fixed:** A dragged board card stays under the pointer, and a faded copy holds its original slot during the drag.
