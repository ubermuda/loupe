- (#449) — **Added:** A column rename that changes the slug writes a `board.column_renamed` outbox event, and every column delete writes one `board.column_deleted` event that names the target column and the moved cards.
