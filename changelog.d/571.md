- (#571) — **Fixed:** The bridge resumes a worker that ends its turn before its work is done, up to `maxResumes` times per rule, and marks the run Gave up at the cap.
- (#571) — **Added:** Worker runs record a structured result, show the states Unfinished, Blocked and Gave up, and a board card warns when its latest run gave up or is blocked.
