- (#476) — **Added:** a project now has a **Worker runs** page that lists what a CLI bridge reported about the Claude Code workers it ran. A row shows the card, the rule, when the run started, how long it took and how it ended, and every run carries its full output collapsed under it. The list pages at 20, searches the card number, the rule name and the output text, and filters by outcome and by bridge. The page states that a missing record means unknown and never means that the worker did not run.
