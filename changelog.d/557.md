- (#557) — **Fixed:** a self-hosted instance on DigitalOcean App Platform reaches its Mercure hub. The ingress stripped the matched path prefix, so the hub received `/` and answered its welcome page, and `loupe bridge run` reconnected in a loop without reporting an error.
