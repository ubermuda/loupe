- (#435) — **Changed:** Ten controllers now delegate their work to a Command/Handler pair, so the wizard steps, the connect page, the social login start, the export download, the About page and the dev preview login each have a name outside the request.
