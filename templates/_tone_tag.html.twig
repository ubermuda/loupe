{#- A coloured label. Props: label (translated text), tone (a LabelTone value), class (optional). -#}
<span class="lp-tag lp-tag--{{ tone }}{{ class|default('') ? ' ' ~ class }}" data-tone="{{ tone }}">{{ label }}</span>
