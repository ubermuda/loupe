- (#556) — **Fixed:** an instance with no Mercure hub answers 404 on `GET /api/events` instead of 500. The endpoint is meant to report that push is switched off, and it could not be reached at all: the Mercure token factory reads `MERCURE_JWT_SECRET` while it is built, so the controller could not be constructed. `loupe bridge run` reported an opaque server error against such an instance.
