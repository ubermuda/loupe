- (#463) — **Changed:** the board documentation, the `loupe-board` skill and the
  landing page describe the columns each board sets, and the skill tells an
  agent to read `board_columns` before it names a column.
