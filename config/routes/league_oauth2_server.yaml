# The authorize endpoint is App\Module\OAuth\Controller\AuthorizeController.
# The bundle's route file is not imported, because it also exposes the device
# code endpoint, which no enabled grant serves.
oauth2_token:
    path: /oauth/token
    controller: league.oauth2_server.controller.token::indexAction
    methods: [POST]
