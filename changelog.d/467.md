- (#467) — **Changed:** `GET /api/events` returns the hub URL, your user topic, one subscriber JWT for that topic, and the projects you own.
- (#467) — **Changed:** Loupe publishes each project event on the project owner's user topic as well as on the project topic.
- (#467) — **Removed:** `GET /api/projects/{handle}/stream` is gone, so every loupe CLI built before this change, including one built after #459, gets 404 and must be rebuilt.
- (#467) — **Changed:** one CLI bridge follows every project in its rule file on one connection, and ignores the events of a project the file does not map.
- (#467) — **Changed:** a project created while the CLI bridge runs reaches it with no restart, and the bridge logs it once and ignores it until you map it.
- (#467) — **Added:** the CLI bridge logs `project_gone` once when a mapped project is deleted or stops being yours, with the rules that stop working.
- (#467) — **Added:** `GET /api/projects` carries each project's slug, and the CLI bridge lists your slugs when a rule file names a project you do not own.
