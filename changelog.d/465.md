- (#465) — **Fixed:** the CLI bridge runs a rule prompt that starts with `-`,
  because it passes the prompt to `claude` after `--`.
- (#465) — **Changed:** the CLI bridge refuses to start when a `permissionMode`
  is not a mode `claude` accepts, or a `model` holds whitespace, in the rule
  file or in `--permission-mode` and `--model`.
