{% apply inline_css(source('@EmailStyles/email.css')) %}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ 'account.email.reset_password.html_title'|trans }}</title>
</head>
<body>
    <div class="brand">
        <span class="brand-mark"></span><span class="brand-name">{{ 'nav.brand'|trans }}</span>
    </div>
    <div class="card">
        <h1>{{ 'account.email.reset_password.heading'|trans }}</h1>
        <p class="body-text">{{ 'account.email.reset_password.body'|trans }}</p>
        <a href="{{ resetUrl }}" class="btn">{{ 'account.email.reset_password.cta'|trans }}</a>
        <p class="footer">
            {{ 'account.email.reset_password.footer_disclaimer'|trans }}<br>
            {{ 'account.email.reset_password.footer_copy_link'|trans }} {{ resetUrl }}
        </p>
    </div>
</body>
</html>
{% endapply %}
