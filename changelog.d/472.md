- (#472) — **Added:** the CLI bridge marks a rule dead when a column rename, a
  column delete or a project rename takes away a slug the rule names. A dead
  rule starts no worker until the bridge restarts.
- (#472) — **Added:** the CLI bridge reports the health of its rules to the
  server at start and whenever a rule dies, without prompt text. It keeps a
  bridge id in `config.json` for this.
