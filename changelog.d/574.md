- (#574) — **Added:** The Loupe CLI updates itself to the newest verified release that the instance supports, and a running bridge keeps its workers through the update.
- (#574) — **Added:** `loupe update` updates the CLI by hand, and `autoUpdate: false` in the rule file turns automatic updates off.
- (#574) — **Added:** The agents list shows the update state of each bridge, and warns when a bridge runs a CLI version that the instance does not support.
- (#574) — **Changed:** The bridge heartbeat answers 200 with the supported CLI range, in place of 204.
- (#574) — **Removed:** The CLI no longer supports Windows.
