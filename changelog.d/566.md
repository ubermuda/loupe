- (#566) — **Added:** The Worker runs page and the card page show each state of a bridge worker run, from queued to its outcome, with the history of states in the drawer, and they update live.
- (#566) — **Added:** A run whose bridge goes quiet reads Timed out, and a run that a restarted bridge no longer holds reads Lost.
- (#566) — **Changed:** The bridge run report limit is 240 requests a minute for each token, up from 60.
