- (#512) — **Changed:** The document review page now matches the approved redesign: a compact header with a status byline, icon margin tabs, restyled comment cards and decisions, a linked board card in Details, a one-row diff toolbar, and a History table with per-version discussion counts.
- (#512) — **Changed:** The comment margin opens on its Open filter, so resolving a thread takes its card out of the column, and Details lists only the rows the document has.
- (#512) — **Removed:** The Deleted threads page, the permanent purge and Copy review summary. Deleting a thread keeps its confirmation and its immediate Undo.
- (#512) — **Removed:** The diff view's compare chip, metadata tabs and contents rail; the diff now carries the same margin as the document view.
