- (#452) — **Changed:** a CLI bridge event for a card whose worker still runs waits and runs after that worker exits, once per rule however many events arrived, and logs `worker_coalesced` for each event it replaced.
- (#452) — **Added:** each bridge rule's `maxChain`, three by default, caps the runs in a row that agents' events start for one card, until a person moves the card.
