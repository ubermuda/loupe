- (#539) — **Added:** `loupe init` now checks how Claude Code starts the `loupe` MCP server, and offers to declare `loupe mcp` for every project when nothing does. One declaration serves every repository, because the working directory chooses the project through `.loupe.yaml`.
- (#539) — **Added:** `loupe init` reports a declaration that starts something else, and offers to remove it. Removing always asks, so a script never drops one you made by hand.
- (#539) — **Added:** `loupe init --mcp-json` writes `.mcp.json` instead, to commit the server to the repository. Every other server in that file is kept, and init says when a higher-scoped declaration hides it.
