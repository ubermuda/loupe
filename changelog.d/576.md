- (#576) — **Changed:** each site-review widget note is now feedback on a board card. The widget asks once per project for a mode: one card per note, one card per review, or an epic with one card per note.
- (#576) — **Added:** a "Site review" card type, and a card that moves to a terminal column resolves its open feedback.
- (#576) — **Added:** the MCP tools `feedback_list` and `feedback_mark_addressed`, and `card_get` returns each feedback item in full.
- (#576) — **Removed:** the site-review inbox page, its attach-to-card flow, `site_reviews.json` in the data export, and the MCP tools `site_review_get` and `site_review_mark_comment_addressed`.
- (#576) — **Changed:** the board now ships on. The upgrade turns `board.enabled` on for an existing instance.
- (#576) — **Removed:** the upgrade deletes every existing site-review comment, including comments already on a card. This cannot be undone, so back up the database first if you need them.
