- (#437) — **Changed:** `loupe bridge run` now runs a non-interactive `claude -p` worker for each board card moved to `next`, and reports its exit code and its output.
- (#437) — **Changed:** `loupe bridge run` requires `--dir`, and refuses a path that is not a directory.
- (#437) — **Removed:** `loupe bridge run` drops tmux, `--session`, `--attach` and the bridge log, and no longer acts on a submitted site review.
- (#437) — **Fixed:** a card whose worker has finished starts a new worker the next time it moves to `next`.
