- (#431) — **Changed:** the `working-with-prs` skill carries the rule that a
  check is a fault until you have seen it fail, with the `comm` instrument for
  proving that a conflict resolution lost or invented nothing.
- (#431) — **Changed:** the `loupe-board` skill says what earns a card. A
  lesson belongs in the skill that covers it rather than in a card nobody can
  finish.
