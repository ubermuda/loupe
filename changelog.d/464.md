- (#464) — **Added:** the server accepts bridge rule health reports at `PUT /api/projects/{handle}/bridges/{bridgeId}/rules`, the board shows the owner a banner for each dead rule, and the column rename and delete dialogs warn when a live rule watches the column. The CLI bridge does not send reports yet.
