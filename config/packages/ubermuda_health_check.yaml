ubermuda_health_check:
    # `default::` resolves an unset or blank variable to null, which the bundle
    # types as a string, so the cast is load-bearing. Blank means /healthz
    # reports no sensitive metadata to anyone.
    probe_token: '%env(string:default::HEALTH_PROBE_TOKEN)%'
