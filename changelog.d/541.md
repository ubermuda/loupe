- (#541) — **Removed:** the Claude Code plugin no longer declares an MCP server, so installing it asks for no project API token. `loupe login` and `loupe init` set the server up instead, which binds per project through `.loupe.yaml` rather than holding one token for every project at once.
- (#541) — **Added:** the plugin now ships all eight `loupe-*` skills, including the board, the inbox and the four card stages. It previously shipped two.
