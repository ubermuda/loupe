- (#504) — **Added:** a bridge rule on `inbox.ask_closed` with `resume: true` resumes the Claude Code session that asked, on the card of that session, and skips the resume when the session already read every item of its ask.
