- (#544) — **Fixed:** `loupe bridge` accepts the `system` actor. Loupe writes it on the card move that follows an approved document, and the router dropped that event as malformed, so an approval moved a card no worker saw.
