# No per-stack environment overrides: the app reads its configuration from the
# .env chain as normal. Mounted by the main stack and by every worktree stack.
# compose.e2e.yaml mounts a generated file here instead — see `just e2e-up`.
