- (#567) — **Added:** A board card links to another card of its project as "related to", "blocks" or "blocked by". The card page, the card form and the `relatedCards` field of the MCP board tools show and write these links.
- (#567) — **Fixed:** `just worktree-up` no longer deletes the committed files under `assets/vendor` in a worktree.
