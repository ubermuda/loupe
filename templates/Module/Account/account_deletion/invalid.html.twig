{% extends '@Account/auth_base.html.twig' %}

{% block title %}{{ 'account.delete.invalid.page.title'|trans }} — {{ 'app.name'|trans }}{% endblock %}

{% block auth_body %}
<div class="flex flex-col items-center gap-3 py-4 text-center">
    <div class="auth-icon-badge">
        <twig:UX:Icon name="lucide:link-2-off" class="w-5 h-5" aria-hidden="true" />
    </div>
    <div>
        <h1 class="text-xl font-semibold tracking-tight text-fg">{{ 'account.delete.invalid.heading'|trans }}</h1>
        <p class="text-fg-mute text-sm mt-1.5 leading-normal">{{ 'account.delete.invalid.body'|trans }}</p>
    </div>

    <a href="{{ path('app_login') }}" class="text-sm text-accent font-medium hover:underline mt-1">{{ 'account.delete.invalid.back'|trans }}</a>
</div>
{% endblock %}
