- (#472) — **Added:** the CLI bridge stops a rule until restart when a column rename, a column delete or a project rename takes away a slug the rule names.
- (#472) — **Added:** the CLI bridge reports the health of its rules to the server at start and whenever a rule dies, without prompt text.
- (#472) — **Changed:** the CLI bridge refuses at start a rule file whose health report the server would reject, such as a rule name longer than 100 characters.
