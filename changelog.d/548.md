- (#548) — **Added:** Loupe receives forge webhooks at `/webhooks/forge/{forge}`. A merge, a review verdict and a concluded check reach the board on their own, so a card no longer waits for a person to read the forge and drag it.
