- (#432) — **Changed:** every MCP tool now reads and writes through a
  Command/Handler pair, and two PHPStan rules hold the line, so a new tool that
  injects no handler or queries a repository fails the build.
