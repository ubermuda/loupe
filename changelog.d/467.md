- (#467) — **Changed:** `GET /api/events` returns the hub URL, the topic of every
  project you own and one subscriber JWT for all of them. Every loupe CLI binary
  built before this change, including one built after #459, gets 404 and must be
  rebuilt.
- (#467) — **Removed:** `GET /api/projects/{handle}/stream` is gone.
- (#467) — **Changed:** one CLI bridge follows every project in its rule file on
  one connection, and ignores the events of a project the file does not map.
- (#467) — **Added:** `GET /api/projects` carries each project's slug, and the
  CLI bridge lists your slugs when a rule file names a project you do not own.
