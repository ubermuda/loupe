- (#523) — **Added:** `loupe mcp` serves Loupe's MCP tools to a local agent over stdio, signed with your own `loupe login`, so no tool and no configuration file holds a token.
- (#523) — **Added:** `loupe init` writes `.loupe.yaml`, the committed file that names the Loupe project a repository belongs to.
- (#523) — **Fixed:** an agent connected through `loupe mcp` keeps its Loupe tools when the server ends its MCP session. The command opens a new session and replays the handshake.
