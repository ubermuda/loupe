- (#542) — **Removed:** Static API tokens. Every credential now comes from an OAuth grant. `/account/api-tokens` and the project token pages are gone, and `loupe login --token` and `LOUPE_TOKEN` go with them.
- (#542) — **Removed:** `SITE_REVIEW_WIDGET_TOKEN`, with its Terraform variable and its compose slot. Use `SITE_REVIEW_WIDGET_PROJECT`.
- (#542) — **Changed:** The site-review widget reads `data-project` only. A page that still carries `data-token` serves no widget, so re-embed it with the project id from the project's Connect page.
- (#542) — **Changed:** The audit trail names the credential that acted through a new `oauth_credentials` row, which survives a token refresh.
- (#542) — **Changed:** The Connect page and the first-run wizard hand out no credential. They show the OAuth connect commands instead.
