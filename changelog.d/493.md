- (#493) — **Added:** the bridge sends a heartbeat to `PUT /api/bridges/{bridgeId}/heartbeat` at start and then at an interval, so Loupe records which bridges run. The `bridge.heartbeat_interval_seconds` flag sets the interval, 60 seconds by default.
