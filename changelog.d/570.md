- (#570) — **Added:** An interactive session can register on a card with the `card_run_open` and `card_run_close` MCP tools, and the card drawer shows the open run with a close control.
- (#570) — **Added:** A bridge rule takes `card: { interactiveRun: false }` to skip a card that a person works in an interactive session.
