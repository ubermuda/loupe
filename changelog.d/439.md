- (#439) — **Added:** The CLI bridge runs at most `--max-workers` workers at once, three by default, and queues the rest in arrival order.
- (#439) — **Changed:** The CLI bridge writes one JSON object per line, to stdout and to a log file named by `--log-file`, and appends to that file across runs.
