- (#441) — **Added:** API tokens gain an `agent` scope, and the account settings page now mints it instead of `site-review`.
- (#441) — **Changed:** the loupe CLI reads `GET /api/agent/sites` and `GET /api/agent/stream`, which replace `GET /api/site-review/sites` and `GET /api/site-review/stream`.
- (#441) — **Changed:** the firewall grants `^/api/agent` to the agent scope alone, so a project's widget token is now refused before the controller runs rather than by the controller.
- (#441) — **Changed:** an audit record written through an agent token reports the `agent` channel rather than `widget`.
- (#441) — **Removed:** an account-level API token minted before this release stops working on the CLI endpoints, and has to be minted again from `/account`.
- (#441) — **Changed:** no widget token is affected by the scope split, and no widget deployment changes.
