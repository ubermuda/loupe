- (#486) — **Changed:** the `holding-a-merge-queue`, `project-worktrees` and
  `working-with-prs` skills record the merge queue lessons of 2026-09-14,
  including how to read an approval by time and why a worktree must never run
  `npm ci`.
