- (#445) — **Added:** the `board_columns` MCP tool lists the columns of the
  board, with each slug, label and terminal and default flag.
- (#445) — **Changed:** `card_list` returns the columns of the board beside its
  cards, and the board tools no longer describe a fixed set of four columns.
