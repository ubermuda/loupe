- (#562) — **Fixed:** The bridge no longer lets `claude -p` end a worker after 600 seconds while a background subagent still runs.
- (#562) — **Fixed:** A worker that ends with no `STAGE RESULT:` line now logs `worker_no_result` and shows **No result** on the Worker runs page, where it read as a success before.
