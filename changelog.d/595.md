- (#595) — **Changed:** the Amphetamine bridge hook ends any session at idle during `quiet_hours` or after `away_minutes` with no input, and otherwise makes it a timed session that ends when quiet hours start. The `takeover` setting is gone.
- (#595) — **Fixed:** a hook package that a bridge reload adds while the bridge is busy now gets the idle that follows.
