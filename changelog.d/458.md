- (#458) — **Changed:** the loupe CLI reads `GET /api/projects` and `GET /api/projects/{handle}/stream`, which replace `GET /api/agent/sites` and `GET /api/agent/stream?site=<handle>`.
- (#458) — **Removed:** `GET /api/agent/sites` and `GET /api/agent/stream` are gone, so a loupe CLI built before this change gets 404 and must be upgraded.
