- (#529) — **Added:** one Loupe login now reaches both the bridge endpoints and the MCP endpoint, so `loupe mcp` works from the same sign-in as `loupe bridge`.
- (#529) — **Added:** the consent screen says when an app is asking for every project you own, including ones you create later.
- (#529) — **Changed:** the connected-apps entry in your data export now lists `scopes` rather than one `scope`, with an `allProjects` flag beside it.
