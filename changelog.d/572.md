- (#572) — **Added:** a card can be an epic, and a card can have one epic as its parent. The epic page lists its children with a progress count such as "3/7 done".
- (#572) — **Added:** the board shows each epic as a swimlane across the columns. Each lane has a collapse control for each browser and a lane toggle that the epic stores. A drag into another lane changes the parent of the card.
- (#572) — **Added:** the app closes an epic when all its children are done, and reopens it when a child reopens. It moves a blocked child to Implementation when its last blocker is done.
- (#572) — **Added:** the MCP card tools read and write the parent and the lane toggle, and `card_list` filters by `parentCardId`.
- (#572) — **Added:** the tech design stage can propose a Breakdown section, and the implementation stage then creates the child cards instead of code.
