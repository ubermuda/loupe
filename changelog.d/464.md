- (#464) — **Added:** a bridge reports its rule health through `PUT /api/projects/{handle}/bridges/{bridgeId}/rules`, the board shows the owner a banner for each dead rule, and the column rename and delete dialogs warn when a live rule watches the column.
