- (#442) — **Added:** every project has a slug that follows its name, unique per owner, shown read-only on the project settings page.
- (#442) — **Changed:** a project name that gives an empty slug, or the slug of another of your projects, is refused.
- (#442) — **Changed:** `GET /api/site-review/stream` and the MCP `site` parameter accept a project slug beside the id and the name.
- (#442) — **Fixed:** the development PHP image builds xdebug without contacting pecl.php.net, so an outage there no longer fails the build.
