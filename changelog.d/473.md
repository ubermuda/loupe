- (#473) — **Added:** The `loupe` CLI gives each machine's bridge a persistent id, stored in `config.json` and generated on first use.
- (#473) — **Changed:** The `loupe` CLI writes `config.json` through a temporary file and a rename, so a failed write no longer leaves the login half written.
