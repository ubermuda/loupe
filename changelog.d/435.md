- (#435) — **Changed:** every controller that injects a collaborator now
  delegates through a Command/Handler pair, and a PHPStan rule holds the line,
  so a new controller that keeps its work in the request fails the build.
