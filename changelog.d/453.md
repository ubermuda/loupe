- (#453) — **Added:** An open board reloads its columns when someone adds, renames, reorders, flags or deletes a column, through the Mercure hub, and an instance with no hub shows the change on the next page load.
